[Unit]
Description=LuxQuant crypto_news retention (prune rows older than 7 days)
After=network.target postgresql.service

[Service]
Type=oneshot
User=postgres
ExecStart=/usr/bin/psql -d luxquant -c "SELECT prune_crypto_news(7)"

[Install]
WantedBy=multi-user.target
